#pragma once

#include "Point.h"

// Describes an axis-aligned rectangle with a velocity
class Box
{
private:
	// Position of top-left corner, dimensions and velocity
	Point<float> point;
//	float x, y, width, height, moveX, moveY;
//public:
//	float SweptAABB(Box obstacle, float& normalX, float& normalY);
};